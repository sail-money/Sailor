# Note: the `block-exotic-subdeps` setting (needed to allow viem's `ox`
# transitive dependency, which wevm resolves via a URL) is NOT read from
# .npmrc by pnpm 11 — it lives in pnpm-workspace.yaml as `blockExoticSubdeps`.
