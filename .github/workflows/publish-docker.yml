name: Publish to Docker Hub

on:
  workflow_call:
    inputs:
      environment:
        description: "'dev' → fungistudio/sailor:dev, 'prod' → fungistudio/sailor:<ver> + :latest"
        type: string
        required: true
      version:
        description: 'Git ref name (e.g. v1.2.3 for tags)'
        type: string
        default: ''
  workflow_dispatch:
    inputs:
      environment:
        description: "'dev' → fungistudio/sailor:dev, 'prod' → fungistudio/sailor:<ver> + :latest"
        type: choice
        options: [dev, prod]
        default: dev
      version:
        description: 'Version tag (e.g. v1.2.3) — required for prod'
        type: string
        default: ''

jobs:
  tests:
    # Skipped when called from the orchestrator (workflow_call already ran tests).
    if: github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/tests.yml

  publish-dev:
    needs: tests
    # always() prevents auto-skip when tests is skipped (workflow_call path).
    if: always() && inputs.environment == 'dev' && (needs.tests.result == 'success' || needs.tests.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          platforms: linux/amd64,linux/arm64
          push: true
          tags: fungistudio/sailor:dev
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish-prod:
    needs: tests
    # always() prevents auto-skip when tests is skipped (workflow_call path).
    if: always() && inputs.environment == 'prod' && (needs.tests.result == 'success' || needs.tests.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Extract version
        id: v
        run: echo "version=${INPUT_VERSION#v}" >> $GITHUB_OUTPUT
        env:
          INPUT_VERSION: ${{ inputs.version }}
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            fungistudio/sailor:${{ steps.v.outputs.version }}
            fungistudio/sailor:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
