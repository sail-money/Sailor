{
  "name": "sailor",
  "private": true,
  "version": "0.0.1",
  "description": "Operator toolkit for Sail Protocol",
  "bin": {
    "sailor": "packages/cli/dist/index.cjs"
  },
  "files": [
    "packages/cli/dist",
    "templates",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm --filter @sail/chains build && pnpm --filter @sail/sdk build && pnpm --filter sailor build && pnpm --filter sailor-ui build",
    "link:cli": "pnpm link --global",
    "typecheck": "pnpm --filter @sail/sdk build && pnpm --filter @sail/chains build && pnpm -r typecheck",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.5.0"
  }
}
