{
  "name": "sailor",
  "private": true,
  "version": "0.0.1",
  "description": "Operator toolkit for Sail Protocol",
  "scripts": {
    "build": "pnpm --filter @sail/sdk build && pnpm --filter '!@sail/sdk' -r build",
    "typecheck": "pnpm --filter @sail/sdk build && pnpm -r typecheck",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.5.0"
  }
}
