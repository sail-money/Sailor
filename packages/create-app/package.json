{
  "name": "create-sailor-agent",
  "version": "0.1.0",
  "description": "Scaffold a new Sail Protocol agent — npx create-sailor-agent my-agent",
  "type": "module",
  "bin": {
    "create-sailor-agent": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "sailor": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
