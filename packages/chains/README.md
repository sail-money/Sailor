# @sail/chains

Per-chain SailKernel deployment addresses.

Exports a `chains` map keyed by `chainId`. Sail Protocol is EVM-compatible — any EVM chain
may be added once `SailKernel` is deployed there and the addresses are verified.

Currently empty: no kernel has been deployed. Populate as deployments land.
