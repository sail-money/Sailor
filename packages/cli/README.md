# sailor

CLI operator toolkit for Sail Protocol. Run `sailor --help` to see all commands.

`sailor init [name]` scaffolds a new DCA-rebalancer agent from the built-in template.
All other commands are stubbed and will print "not implemented yet" until the next phase.

```sh
npm install -g sailor
sailor init my-fund
```
