# Sail permission model: conjunctive vs selective

> **Canonical copy:** [`templates/dca-rebalancer/docs/PERMISSION_MODEL.md`](../templates/dca-rebalancer/docs/PERMISSION_MODEL.md)
>
> The conjunctive-vs-selective dispatch model — the single most important thing to
> understand before operating an SMA — lives in the scaffold template so it ships
> into every user's project (`sailor init` copies the whole template; the repo root
> is **not** part of the published npm package).
>
> Edit the template copy. This file is a pointer so the two never drift.
