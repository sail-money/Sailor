# Sail agent playbook

> **Canonical copy:** [`templates/dca-rebalancer/AGENT_PLAYBOOK.md`](templates/dca-rebalancer/AGENT_PLAYBOOK.md)
>
> The operational playbook — capability detection, the act-on-the-SMA decision tree,
> approval/swap guidance, and the revert failure-mode catalog — lives in the
> scaffold template so it ships into every user's project (`sailor init` copies the
> whole template; the repo root is **not** part of the published npm package).
>
> Edit the template copy. This file is a pointer so the two never drift.

For the permission model that underpins the playbook, see
[`templates/dca-rebalancer/docs/PERMISSION_MODEL.md`](templates/dca-rebalancer/docs/PERMISSION_MODEL.md).
